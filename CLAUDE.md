# CLAUDE.md

## Git commits

Never add a `Co-Authored-By: Claude ...` (or any AI co-author) trailer to
git commit messages in this repo. End the commit message with just the
descriptive body — no trailing `Co-Authored-By` line.
