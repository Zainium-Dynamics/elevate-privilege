[package]
name = "pam-listfile"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "elevate-pam listfile module (file allow/deny list)"
publish = false

[lib]
name = "pam_listfile"
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
elevate-pam = { workspace = true }
libc = { workspace = true }
