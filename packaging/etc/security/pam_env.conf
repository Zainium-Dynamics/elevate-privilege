# /etc/security/pam_env.conf — used by elevate-pam env module
# Format: VARIABLE  [DEFAULT=value]  [OVERRIDE=value]
#
# Examples:
# PATH            DEFAULT=${PATH}        OVERRIDE=/bin:/sbin
# LANG            DEFAULT=C.UTF-8

# Default: empty. Set site-wide environment here if needed.
